# Starship Prompt Config
add_newline = true

[directory]
truncation_length = 8
truncation_symbol = ".../"

[git_status]
format = '([\($all_status$ahead_behind\)]($style) )'
ahead = "⇡\${count}"
behind = "⇣\${count}"
diverged = "⇕⇡\${ahead_count} ⇣\${behind_count}"

[package]
disabled = true
